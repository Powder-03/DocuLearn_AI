{
  "name": "doculearn-ai-lambda",
  "version": "2.1.0",
  "description": "DocuLearn AI - AWS Lambda Deployment",
  "scripts": {
    "deploy": "serverless deploy",
    "remove": "serverless remove",
    "logs": "serverless logs -f api -t",
    "invoke": "serverless invoke -f api"
  },
  "devDependencies": {
    "serverless": "^3.38.0",
    "serverless-python-requirements": "^6.1.0"
  }
}
